#ifndef LOOKUP_H

#include "lutd.h"

/* other types of lookup tables */
typedef struct Node Node;
LUTD_INCLUDE(TNode, tnode, Node, BY_REF);

#if 0
LUTD_INCLUDE(TLink, tlink, Link, BY_REF);
#endif

#define LOOKUP_H
#endif

